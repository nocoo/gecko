# AI E2E credentials — copy to .env.e2e and fill in real values.
# The "AI integration (real LLM)" E2E tests skip when these are absent.
#
# Any Anthropic-compatible or OpenAI-compatible endpoint works.
# sdkType is inferred from the provider or set via the API.
AI_E2E_AUTH_TOKEN=
AI_E2E_BASE_URL=
AI_E2E_MODEL=
