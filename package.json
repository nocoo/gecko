{
  "name": "gecko",
  "version": "1.10.3",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd apps/web-dashboard dev",
    "prepare": "husky",
    "release": "bun run scripts/release.ts",
    "typecheck": "tsc --noEmit -p apps/web-dashboard/tsconfig.json",
    "test:l2": "bun run --cwd apps/web-dashboard vitest run src/__tests__/api/",
    "test:e2e:bdd": "bun run --cwd apps/web-dashboard test:bdd"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
