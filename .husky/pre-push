# L2: API E2E (local SQLite — no remote D1 needed)
# G2: Security gate (osv-scanner + gitleaks)
# L2 & G2 run in parallel

cd apps/web-dashboard

bun run test:e2e &
PID_L2=$!

bun run gate:security &
PID_G2=$!

FAIL=0
wait $PID_L2 || FAIL=1
wait $PID_G2 || FAIL=1

exit $FAIL
